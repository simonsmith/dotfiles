# ~/.tmuxinator/readstack_web.yml

name: readstack_web
root: ~/Sites/readstack/web
startup_window: nvim

windows:
  - nvim: nvim
  - git:
    -
  - server:
      layout: main-horizontal
      panes:
        - php artisan queue:work
        - php artisan serve
        - bun dev
