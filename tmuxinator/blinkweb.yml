name: blinkweb
root: ~/blink/web
startup_window: neovim

windows:
  - neovim:
    - nvim
  - dev:
      layout: main-horizontal
      panes:
        - gs
        -
        -
  - proxy:
    - p proxy --backend staging
