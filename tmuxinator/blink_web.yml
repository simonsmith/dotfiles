name: blink_web
root: ~/blink/web
startup_window: neovim

windows:
  - neovim:
    - nvim
  - dev:
      layout: main-horizontal
      panes:
        - p webapp
        - gs
        -
  - proxy:
    - p proxy --backend staging
