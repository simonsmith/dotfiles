name: blink_web
root: ~/blink/web
startup_window: neovim

windows:
  - neovim:
    - nvim
  - dev:
      layout: tiled
      panes:
        - p webapp
        -
        - gs
        - claude
  - proxy:
    - p proxy --backend staging
