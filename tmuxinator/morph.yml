# ~/.tmuxinator/morph.yml

name: morph
root: ~/Sites/bbc/morph-modules
startup_window: vim

windows:
  - vim:
    - vim --cmd "let g:rooter_patterns = ['package.json', '.git']"
  - git:
    - nvm use 0.12.7
