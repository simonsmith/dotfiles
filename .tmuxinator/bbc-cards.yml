# ~/.tmuxinator/bbc-cards.yml

name: bbc-cards
root: ~/Sites/bbc/bbc-cards
startup_window: vim

windows:
  - vim:
    - vim
  - git:
    - git --no-pager log --oneline --decorate --color --graph -10
    - git status
  - middleman:
    - gulp serve-docs
  - gulp:
    - gulp watch
