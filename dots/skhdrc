# define modes
:: prefix

# activate prefix mode
ctrl - f ; prefix

# disable modes with escape
prefix < escape ; default

# open a terminal
prefix < return : open -na /Applications/Alacritty.app; skhd -k "escape"